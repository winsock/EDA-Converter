{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Debug",
			"type": "gdb",
			"request": "launch",
            "arguments": "$(find ./openjson -name \"*.upv\" -printf \"./openjson/%f \")",
			"target": "./converter",
			"cwd": "${workspaceRoot}/bin"
		}
	]
}